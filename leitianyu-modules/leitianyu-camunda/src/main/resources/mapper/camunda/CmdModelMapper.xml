<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jijia.camunda.mapper.newM.CmdModelMapper">

    <resultMap type="CmdModel" id="CmdModelResult">
        <result property="modelId"    column="model_id"    />
        <result property="name"    column="name"    />
        <result property="categoryId"    column="category_id"    />
        <result property="key"    column="key"    />
        <result property="version"    column="version"    />
        <result property="bpmnXml"    column="bpmn_xml"    />
        <result property="deploymentId"    column="deployment_id"    />
        <result property="formId"    column="form_id"    />
        <result property="description"    column="description"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectCmdModelVo">
        select model_id, name, category_id, key, version, bpmn_xml, deployment_id, form_id, description, del_flag, create_by, create_time, update_by, update_time from cmd_model
    </sql>

    <select id="selectCmdModelList" parameterType="CmdModel" resultMap="CmdModelResult">
        <include refid="selectCmdModelVo"/>
        <where>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="categoryId != null "> and category_id = #{categoryId}</if>
            <if test="key != null  and key != ''"> and key = #{key}</if>
            <if test="version != null "> and version = #{version}</if>
            <if test="bpmnXml != null  and bpmnXml != ''"> and bpmn_xml = #{bpmnXml}</if>
            <if test="deploymentId != null  and deploymentId != ''"> and deployment_id = #{deploymentId}</if>
            <if test="formId != null "> and form_id = #{formId}</if>
            <if test="description != null  and description != ''"> and description = #{description}</if>
        </where>
    </select>
</mapper>
